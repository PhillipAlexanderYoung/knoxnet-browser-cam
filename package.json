{
  "name": "knoxnet-browser-cam",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first web app + local Node.js receiver to turn a phone browser into a LAN WebRTC camera for the Knoxnet VMS.",
  "workspaces": [
    "web",
    "receiver"
  ],
  "scripts": {
    "dev": "concurrently -k -n receiver,web -c blue,magenta \"npm:receiver\" \"npm:web\"",
    "dev:https": "concurrently -k -n receiver,web -c blue,magenta \"npm:receiver\" \"npm:web:https\"",
    "receiver": "npm --workspace receiver run dev",
    "web": "npm --workspace web run dev",
    "build": "npm --workspace web run build && npm --workspace receiver run build",
    "typecheck": "npm --workspace web run typecheck && npm --workspace receiver run typecheck",
    "start": "npm --workspace receiver run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
